FROM llama3.1:8b
ADAPTER ./mymodel

# Parameters
PARAMETER temperature 0.7
PARAMETER top_p 0.9
PARAMETER stop "<|start_header_id|>"
PARAMETER stop "<|end_header_id|>"
PARAMETER stop "<|eot_id|>"

# System Prompt
SYSTEM """Eres Securetag AI, un asistente especializado en ciberseguridad ofensiva y defensiva, auditoría de sistemas y cumplimiento normativo.
Tu conocimiento se basa en estándares y metodologías reconocidas como OWASP (Top 10, API, Proactive Controls), NIST (SP 800-53, CSF, SP 800-61), CIS Controls, ISO 27001, PCI DSS, PTES, MITRE ATT&CK/D3FEND, y técnicas de hacking ético (HackTricks, MASTG).
Responde a las consultas técnicas con precisión, utilizando terminología adecuada y proporcionando ejemplos prácticos o comandos cuando sea relevante.
Tus respuestas deben ser en español, profesionales, directas y estructuradas (usando Markdown)."""

# Template (Llama 3.1 Official)
TEMPLATE """{{- if or .System .Tools }}<|start_header_id|>system<|end_header_id|>
{{- if .System }}

{{ .System }}
{{- end }}
{{- if .Tools }}

Cutting Knowledge Date: December 2023

When you receive a tool call response, use the output to format an answer to the orginal user question.

You are a helpful assistant with tool calling capabilities.
{{- end }}<|eot_id|>
{{- end }}
{{- range $i, $_ := .Messages }}
{{- $last := eq (len (slice $.Messages $i)) 1 }}
{{- if eq .Role "user" }}<|start_header_id|>user<|end_header_id|>
{{- if and $.Tools $last }}

Given the following functions, please respond with a JSON for a function call with its proper arguments that best answers the given prompt.

Respond in the format {"name": function name, "parameters": dictionary of argument name and its value}. Do not use variables.

{{ range $.Tools }}
{{- . }}
{{ end }}
Question: {{ .Content }}<|eot_id|>
{{- else }}

{{ .Content }}<|eot_id|>
{{- end }}{{ if $last }}<|start_header_id|>assistant<|end_header_id|>

{{ end }}
{{- else if eq .Role "assistant" }}<|start_header_id|>assistant<|end_header_id|>
{{- if .ToolCalls }}
{{ range .ToolCalls }}
{"name": "{{ .Function.Name }}", "parameters": {{ .Function.Arguments }}}{{ end }}
{{- else }}

{{ .Content }}
{{- end }}{{ if not $last }}<|eot_id|>{{ end }}
{{- else if eq .Role "tool" }}<|start_header_id|>ipython<|end_header_id|>

{{ .Content }}<|eot_id|>{{ if $last }}<|start_header_id|>assistant<|end_header_id|>

{{ end }}
{{- end }}
{{- end }}"""